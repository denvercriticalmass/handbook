<%= form_with model: cheat_sheet, url: url do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title, required: true, autofocus: true %>

  <%= form.label :body %>
  <%= form.text_area :body, rows: 16 %>

  <%= form.submit "Save" %>
<% end %>
