<h1>Search</h1>

<%= render "search" %>

<% if @search.term.blank? %>
  <p>Type a word from a guide or cheat sheet.</p>
<% elsif !@search.any? %>
  <p>Nothing matched <%= @search.term %>.</p>
<% end %>

<%= render "results", guides: @search.guides, cheat_sheets: @search.cheat_sheets %>
