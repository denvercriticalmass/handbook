<!DOCTYPE html>

<html lang="en">
  <head>
    <%= render "layouts/head" %>
  </head>

  <body>
    <%= render "layouts/nav" if authenticated? %>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
